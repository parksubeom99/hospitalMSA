# Archived mixed backend artifact

이 폴더는 `csFrontend` 루트에 섞여 있던 Spring Boot(백엔드) 흔적을 보관한 위치입니다.

- `build.gradle`: 프론트(Next.js) 실행에는 필요하지 않은 파일이라 루트에서 분리했습니다.

목적:
- 프론트 루트를 Next.js 기준으로 정리
- 기존 파일은 삭제 대신 보관(추적 가능)
