apiVersion: apps/v1
kind: Deployment
metadata:
  name: admin-master-service
  labels:
    app.kubernetes.io/name: admin-master-service
    app.kubernetes.io/component: app
    {{- include "hospitalmsa.commonLabels" . | nindent 4 }}
spec:
  replicas: {{ .Values.adminMasterService.replicaCount }}
  selector:
    matchLabels:
      app.kubernetes.io/name: admin-master-service
  template:
    metadata:
      labels:
        app.kubernetes.io/name: admin-master-service
        app.kubernetes.io/component: app
    spec:
      enableServiceLinks: false
      containers:
        - name: admin-master-service
          image: {{ .Values.adminMasterService.image.repository }}:{{ .Values.adminMasterService.image.tag }}
          imagePullPolicy: {{ .Values.global.imagePullPolicy }}
          ports:
            - name: http
              containerPort: 8182
          envFrom:
            - configMapRef:
                name: app-config
            - secretRef:
                name: {{ .Values.secrets.app }}
          env:
            - name: ORACLE_USER
              value: {{ .Values.adminMasterService.oracleUser }}
            - name: ORACLE_PASSWORD
              valueFrom:
                secretKeyRef:
                  name: {{ .Values.secrets.oracle }}
                  key: ORACLE_PASSWORD
            - name: IAM_BASE_URL
              value: http://iam-service:8181
            - name: CLINICAL_BASE_URL
              value: http://clinical-service:8183
            - name: SUPPORT_BASE_URL
              value: http://support-service:8184
          startupProbe:
            tcpSocket:
              port: http
            periodSeconds: 10
            failureThreshold: 30
          readinessProbe:
            tcpSocket:
              port: http
            periodSeconds: 10
            failureThreshold: 3
          livenessProbe:
            tcpSocket:
              port: http
            periodSeconds: 15
            failureThreshold: 4
          {{- with .Values.adminMasterService.resources }}
          resources:
            {{- toYaml . | nindent 12 }}
          {{- end }}
---
apiVersion: v1
kind: Service
metadata:
  name: admin-master-service
  labels:
    app.kubernetes.io/name: admin-master-service
    app.kubernetes.io/component: app
    {{- include "hospitalmsa.commonLabels" . | nindent 4 }}
spec:
  type: ClusterIP
  selector:
    app.kubernetes.io/name: admin-master-service
  ports:
    - name: http
      port: 8182
      targetPort: http
