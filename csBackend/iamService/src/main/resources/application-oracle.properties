# =============================================================================
# iamService ? Oracle ????
# ???: --spring.profiles.active=oracle
# [ADDED] MySQL application.properties? ???? Oracle ?? ?????
# =============================================================================

# [MODIFIED] Oracle JDBC URL ? XE ?? SID: XE / ????: XEPDB1(PDB ??)
spring.datasource.url=jdbc:oracle:thin:@${ORACLE_HOST:localhost}:${ORACLE_PORT:1521}/${ORACLE_SID:XEPDB1}
spring.datasource.driver-class-name=oracle.jdbc.OracleDriver
spring.datasource.username=${ORACLE_USER:his_iam}
spring.datasource.password=${ORACLE_PASSWORD:his_password}

# [MODIFIED] Hibernate Dialect ? Oracle 12c+ (IDENTITY ?? ??)
spring.jpa.database-platform=org.hibernate.dialect.OracleDialect
spring.jpa.hibernate.ddl-auto=none
spring.jpa.show-sql=false

# [ADDED] Oracle CLOB/BLOB ?? ??
spring.jpa.properties.hibernate.jdbc.lob.non_contextual_creation=true

# [MODIFIED] Flyway Oracle ??
spring.flyway.enabled=true
spring.flyway.locations=classpath:db/migration
spring.flyway.baseline-on-migrate=true
spring.flyway.baseline-version=0
# [ADDED] Oracle PL/SQL ??(/? ??? ??) ??
# [ADDED] ?? ?? ?? repair ? ??? ? DROP/??? ?? ??
spring.flyway.repair-on-migrate=true
# [ADDED] validate ???? ? ?? ?? ?? ? validate ???? ?? ?? ??
spring.flyway.validate-on-migrate=false

# [ADDED] HikariCP ? Oracle XE ?? ?? ?? (XE: ?? 20 ??)
spring.datasource.hikari.maximum-pool-size=5
spring.datasource.hikari.minimum-idle=2
spring.datasource.hikari.connection-timeout=30000
spring.datasource.hikari.idle-timeout=600000